import * as React from 'react';

interface Item{ 
    selite: string;
    kpl: number;
    hinta: number;
    alv: number;
    price: number;
    total: number;
}

export default Item

// export default interface Items extends Array<Item>{};